<!DOCTYPE html>
<html>

<head>
  <title>Redirecting...</title>
  <style>
    body {
      background: #f8fafc;
      color: #222;
      font-family: 'Inter', Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
    }

    .container {
      text-align: center;
    }

    .spinner {
      margin: 32px auto 16px;
      width: 48px;
      height: 48px;
      border: 6px solid #e5e7eb;
      border-top: 6px solid #2563eb;
      border-radius: 50%;
      animation: spin 1s linear infinite;
    }

    @keyframes spin {
      to {
        transform: rotate(360deg);
      }
    }

    .message {
      font-size: 1.2rem;
      margin-bottom: 8px;
    }

    .small {
      color: #6b7280;
      font-size: 0.95rem;
    }
  </style>
  <script>
    window.onload = function () {
      window.location = '{{ deeplinkUrl }}';
      setTimeout(function () {
        window.location = '{{ fallbackUrl }}';
      }, 100);
    }
  </script>
</head>

<body>
  <div class="container">
    <div class="spinner"></div>
    <div class="message" id="redirect-message">Redirecting...</div>
    <div class="small">If you're not redirected shortly, please click <a href="{{ fallbackUrl }}">here</a>.</div>
  </div>
</body>

</html>